# AI Service (Phase 1)

FastAPI placeholder service for LegalMitra.

## Run locally

```bash
pip install -r requirements.txt
uvicorn main:app --reload --port 8000
```

## Endpoints

- `GET /health`
- `POST /ai/ask` (placeholder response)
